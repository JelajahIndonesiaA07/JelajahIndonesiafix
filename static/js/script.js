
var player;
var players;
var playerss;
var kepseribu;
var masjid;
var museum;
var monkey;
var agung;
var seminyak;
var kelingking;
var waterboom;
var uluwatu;
var malioboro;
var borobudur;
var prambanan;
var boko;
var tamansari;
var pinus;
var perahu;
var papandayan;
var braga;
var kebun;
var sate;
    
    $(document).on('mouseover', '#player', function() {
      player.playVideo();
    });
    $(document).on('mouseout', '#player', function() {
      player.pauseVideo();
    });

    $(document).on('mouseover', '#players', function() {
      players.playVideo();
    });
    $(document).on('mouseout', '#players', function() {
      players.pauseVideo();
    });

    $(document).on('mouseover', '#playerss', function() {
      playerss.playVideo();
    });
    $(document).on('mouseout', '#playerss', function() {
      playerss.pauseVideo();
    });

    $(document).on('mouseover', '#kepseribu', function() {
      kepseribu.playVideo();
    });
    $(document).on('mouseout', '#kepseribu', function() {
      kepseribu.pauseVideo();
    });

    $(document).on('mouseover', '#masjid', function() {
      masjid.playVideo();
    });
    $(document).on('mouseout', '#masjid', function() {
      masjid.pauseVideo();
    });

    $(document).on('mouseover', '#museum', function() {
      museum.playVideo();
    });
    $(document).on('mouseout', '#museum', function() {
      museum.pauseVideo();
    });
    
    $(document).on('mouseover', '#monkey', function() {
      monkey.playVideo();
    });
    $(document).on('mouseout', '#monkey', function() {
      monkey.pauseVideo();
    });

    $(document).on('mouseover', '#agung', function() {
      agung.playVideo();
    });
    $(document).on('mouseout', '#agung', function() {
      agung.pauseVideo();
    });

    $(document).on('mouseover', '#seminyak', function() {
      seminyak.playVideo();
    });
    $(document).on('mouseout', '#seminyak', function() {
      seminyak.pauseVideo();
    });

    $(document).on('mouseover', '#kelingking', function() {
      kelingking.playVideo();
    });
    $(document).on('mouseout', '#kelingking', function() {
      kelingking.pauseVideo();
    });

    $(document).on('mouseover', '#waterboom', function() {
      waterboom.playVideo();
    });
    $(document).on('mouseout', '#waterboom', function() {
      waterboom.pauseVideo();
    });

    $(document).on('mouseover', '#uluwatu', function() {
      uluwatu.playVideo();
    });
    $(document).on('mouseout', '#uluwatu', function() {
      uluwatu.pauseVideo();
    });

    $(document).on('mouseover', '#malioboro', function() {
      malioboro.playVideo();
    });
    $(document).on('mouseout', '#malioboro', function() {
      malioboro.pauseVideo();
    });

    $(document).on('mouseover', '#borobudur', function() {
      borobudur.playVideo();
    });
    $(document).on('mouseout', '#borobudur', function() {
      borobudur.pauseVideo();
    });

    $(document).on('mouseover', '#prambanan', function() {
      prambanan.playVideo();
    });
    $(document).on('mouseout', '#prambanan', function() {
      prambanan.pauseVideo();
    });

    $(document).on('mouseover', '#boko', function() {
      boko.playVideo();
    });
    $(document).on('mouseout', '#boko', function() {
      boko.pauseVideo();
    });

    $(document).on('mouseover', '#tamansari', function() {
      tamansari.playVideo();
    });
    $(document).on('mouseout', '#tamansari', function() {
      tamansari.pauseVideo();
    });

    $(document).on('mouseover', '#pinus', function() {
      pinus.playVideo();
    });
    $(document).on('mouseout', '#pinus', function() {
      pinus.pauseVideo();
    });

    $(document).on('mouseover', '#perahu', function() {
      perahu.playVideo();
    });
    $(document).on('mouseout', '#perahu', function() {
      perahu.pauseVideo();
    });

    $(document).on('mouseover', '#gede', function() {
      gede.playVideo();
    });
    $(document).on('mouseout', '#gede', function() {
      gede.pauseVideo();
    });

    $(document).on('mouseover', '#papandayan', function() {
      papandayan.playVideo();
    });
    $(document).on('mouseout', '#papandayan', function() {
      papandayan.pauseVideo();
    });

    $(document).on('mouseover', '#braga', function() {
      braga.playVideo();
    });
    $(document).on('mouseout', '#braga', function() {
      braga.pauseVideo();
    });

    $(document).on('mouseover', '#kebun', function() {
      kebun.playVideo();
    });
    $(document).on('mouseout', '#kebun', function() {
      kebun.pauseVideo();
    });

    $(document).on('mouseover', '#sate', function() {
      sate.playVideo();
    });
    $(document).on('mouseout', '#sate', function() {
      sate.pauseVideo();
    });

    $(document).ready(function () {
      $("#myModal").modal();
      loadData();
    });

    


    
